import React from 'react';

export const BurgerConstructorContext = React.createContext([]);
export const TotalPriceContext = React.createContext(0);
export const OrderNumContext = React.createContext(0);